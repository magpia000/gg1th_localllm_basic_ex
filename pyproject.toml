[project]
name = "local-llm-app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "ollama>=0.6.2",
    "pydentic>=0.0.1.dev3",
    "requests>=2.34.2",
]
